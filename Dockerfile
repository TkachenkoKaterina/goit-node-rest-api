# Використовуємо офіційний образ Node.js як базовий образ
FROM node:20

# Встановлюємо робочу директорію в контейнері
WORKDIR /app

# Копіюємо package.json і package-lock.json (якщо є) до робочої директорії в контейнері
COPY package*.json ./

# Встановлюємо залежності
RUN npm install

# Копіюємо решту додатку в контейнер
COPY . .

# Відкриваємо порт, який використовується додатком
EXPOSE 3000

# Вказуємо команду для запуску додатка
CMD ["npm", "start"]
